doctype
html
  head
    title hello
    link(rel="stylesheet" href="./style.css")
  body
    .stage
      - for (var x = 0; x < 6; x++)
        .text
          .panel.front
          .panel.top
          .panel.back
          .panel.bottom
          .panel.left
          .panel.right
